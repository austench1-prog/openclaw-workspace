# 2026-06-13 Daily Memory

- Chairman clarified terminology alignment: Dragon’s logical understanding matched his intent, but wording must preserve his original terms. Specifically, use **Fractal** and do not replace it with **分型**.
- New strategy-system material recorded with temporary name: **多策略分布式运用架构 / Multi-Strategy Distributed Execution Architecture**. Core idea: multiple strategies run independently, are distributed across different products or channels, are not fused into the same trade, may run shadow/test logic in parallel without capital, and must route final real orders through one unified execution入口 to avoid internal conflicts and compliance/risk issues.
- Capacity judgment for future automated trading (conceptual, not yet implementation): current stack likely supports about **1–2 real auto strategies comfortably**, **3 as a practical safe upper bound**, and more shadow/sim strategies in parallel, with architecture/risk coordination—not hardware—as the true bottleneck.
- Chairman introduced **Kyle Ng / JadeCap** as a meaningful external benchmark trader. Decision: maintain all Kyle/JadeCap content as a **strictly separate independent reference note**, not mixed into the main system or main trading material library unless explicitly absorbed later.
- Kyle/JadeCap notes emphasized: less-is-more price action, HTF bias, probability/process-over-P&L mindset, PO3, cross-product relative strength, IFVG, poker-chip risk model, emotional journaling, pause rules, edge perishability, and strategic recovery. Dragon’s evaluation: strong professional trader benchmark, useful as reference, not to be worshipped or copied mechanically.
- OpenClaw / Claude investigation progressed: user wants to move away from **claude-fable-5** and determine the highest available Claude/Sonnet level first rather than blindly reverting to Sonnet 4.6.
- Verified with direct API probes that the environment variable **ANTHROPIC_API_KEY** is currently set to a placeholder/present-but-invalid value (prefix observed as `YOUR_NEW_KEY`), and direct calls to `https://api.anthropic.com/v1/models` return **401 authentication_error: invalid x-api-key**. Therefore, Claude API availability and highest accessible model cannot currently be determined until a valid Anthropic API key is installed.
- Current OpenClaw config observations during investigation: `agents.defaults.model.primary = anthropic/claude-fable-5`; fallbacks include `openai/gpt-5.4` and `anthropic/claude-sonnet-4-6`; recent active sessions were effectively using GPT-5.4 rather than Claude. This suggests the system is presently relying on fallback behavior rather than a working Claude path.
