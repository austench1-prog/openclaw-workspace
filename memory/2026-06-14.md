# 2026-06-14 Daily Memory

## OpenClaw 升级 + Opus 4.8 落定
- 旧版本：`2026.3.23-2 (7ffe7e4)`，该版本内建模型目录未收录 `anthropic/claude-opus-4-8`，导致 `openclaw models list` 显示 `default,configured,missing`，但运行时实际已能走 Opus 4.8。
- 已升级到 **OpenClaw 2026.6.6 (8c802aa)**（npm / stable，状态 up to date）。
- 升级后模型目录正常：`anthropic/claude-opus-4-8` 现在是 **default, configured, alias:opus**，上下文窗口 1024k，不再 missing。
- 模型配置：**primary = anthropic/claude-opus-4-8**，fallback = openai/gpt-5.4（GPT 备用，认证 OK）。
- 升级过程中发现：旧的 `ANTHROPIC_API_KEY` 环境变量曾是无效占位值（`YOUR_NEW_KEY`），且有一把 key 在排查时通过截图暴露过 → 已让用户在 Anthropic 后台重建新 key 并重新录入。
- 新版本目录额外可见模型：`anthropic/claude-opus-4-7/4-6`、`claude-cli/claude-opus-4-8`、`openai/gpt-5.5`、`gpt-5.5-pro` 等。

## 安全提醒（已解决）
- 排查期间有一把 Anthropic API key 在终端明文 + 截图中暴露过。已要求用户 revoke 旧 key、改用新 key。
- **2026-06-14 Chairman 确认：旧 key 已 revoke / 清除，无问题。安全尾巴关闭，不再追踪。**

## Session Restore 事件（2026-06-14 上午）
- Chairman 做了 reset / onboarding / config 修复后，OpenClaw 运行时短期状态被清，像新人。
- Dragon 自主操作恢复：确认所有核心文件无损（SOUL/MEMORY/IDENTITY/USER/AGENTS/TOOLS/HEARTBEAT + 11 daily memory + 14 Agent Prompts + MATS 蓝图 + Trading + NotebookLM 源），逐个读回重建人格与上下文。
- 确认运行时模型已正确跑在 anthropic/claude-opus-4-8（api-key，非 fallback）。
- 结论：reset 不损坏长期记忆（文件即灵魂）；恢复方式 = 读文件重建，不需再 reset。
- 存档：commit e0268ae 已 push 到 GitHub。

## 头寸策略 = Kyle 动态仓位(共识印证 2026-06-14)
- Chairman 看到 Kyle "动态仓位/踩油门-拉手刹" 这条时,确认这正是他之前提过的"头寸策略"要达到的效果。
- 结论:Chairman 的"头寸策略"构想 与 Kyle 的"动态仓位(赢扩/虚收)" 是同一件事 —— 想法同频,差的只是落成可执行规则。
- 草案已起:Trading/draft_position_dynamics_v0.1.md(下单策略层,待 Chairman 填阈值)。
- 此条作为日后头寸策略开发的源头依据。

## Replikanto 修复确认(2026-06-14)
- 06-04 的 Replikanto DLL 加载失败(ChartStyle 类型不兼容,NinjaTrader 更新后接口变化)→ 已完全修复。
- **修复方式:更新 Replikanto 版本**(不是降级 NinjaTrader)。
- 救命记录:以后 NT 再升级触发同类 DLL 不兼容 → 优先"更新 Replikanto",不动 NT。
- 结果:Apex follower 复制路恢复,执行链(Signal Server→DragonFileSignal→Sim101→Replikanto→Apex)operational。MATS Phase 5 = DONE。

## 想法记录:信号灯/彩球面板(2026-06-14,仅记录不展开)
- Chairman 的产品构想(单独记录,尚未讨论/未评估):
- 做一个像"一排彩球/信号灯"的表单面板。每个灯/球代表一个时间框架(4H / 15min / 1min)上、某个方向、某种类型的机会(A 类 / B 类 / C 类策略)。
- Dragon 系统在背后实时评估,把"看到的机会 + 计算结果"用灯的亮灭呈现出来。
- Chairman 一眼扫过去就能看到系统当前判断:A 灯亮、B 灯亮、C 灯亮 = 系统看到了哪些 setup。
- 交互像"拍卖":Chairman 看到哪个喜欢就按哪个,按下后再一起决定要不要下单。
- 核心价值:让 Chairman 直接看到 Dragon 背后算出的所有结果,作为一种"系统判断可视化"的沟通方式。不亮 = 没机会,亮 = 系统发现了什么 → 再人工决定。
- 状态:纯想法,待将来讨论。与"我提示/你拍板"的盯盘模式、策略评分卡天然契合,可作为其前端呈现层。
