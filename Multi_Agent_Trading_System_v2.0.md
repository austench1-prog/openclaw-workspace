# 多 Agent 交易系统 v2.0 — 正式更新稿
# Source: 龙哥（基于方案一 / Plan A）
# Version: 2.0 | Date: 2026-04-07
# ⚠️ 不覆盖 v1.0，本文件为正式更新版本

---

## 系统目标

> 既然自动下单已经跑通，就立即把它投入第一阶段小账户使用，同时尽快接入 NotebookLM 解决规则与合规信息准确率不足的问题，让整个执行链真正安全可用。

---

## 模块边界

### 模块 A：Prop Intelligence Agent
**边界：** 只负责收集和输出规则原始信息

- 输入：平台名、阶段、问题类型、官网/FAQ/折扣页
- 输出：当前规则、流程、活动信息、初步风险提示
- 不负责：规则核验、判断对错、执行放行

---

### 模块 B：NotebookLM Compliance Skill
**边界：** 只负责规则信息的准确性核验

- 输入：原始合同截图、官网规则文本、内部规则笔记、问题查询
- 输出：基于来源的规则回答、差异比对、合规风险提示、引用依据
- 不负责：执行下单、策略判断、下单放行
- 定位：**信息准确性增强层，不是执行层**

---

### 模块 C：Strategy Pack
**边界：** 只负责策略 setup 的识别和成熟度判断

- 输入：策略名称、品种、timeframe、当前市场状态
- 输出：setup 状态、已满足/未满足条件、是否接近执行
- 不负责：规则查询、下单执行

---

### 模块 D：Gatekeeper
**边界：** 只负责执行前的最终放行/拦截

- 输入：交易建议、规则核验结果（来自模块 B）、账户状态、风险状态
- 输出：放行 / 拦截 / 降仓 / 需要人工确认 + 原因说明
- 不负责：策略判断、规则查询

---

### 模块 E：Execution
**边界：** 只负责已放行订单的执行

- 输入：已放行订单、下单参数、账户映射
- 输出：执行结果、成交记录、回写状态
- 运行策略：**小账户、可控风险、人工监控，逐步扩大**
- 当前状态：**已跑通，正式投入使用**

---

## 系统内 / 系统外角色

### 系统外：龙哥-A（首席系统工程师）
不参与交易决策链，负责系统稳定运行：

- 硬件监控（CPU、网络延迟、SSH 稳定性）
- 环境维护（编译、日志、GitHub 同步）
- API 熔断（异常时有权停止进程）
- SSH 安全管理

### 系统内：龙哥-B（总裁助理）
参与交易决策链，但只负责组织和传递，不做最终判断：

- 调用模块 A 获取规则信息
- 通过模块 B（NotebookLM）核验准确性后转达
- 整理执行前 Checklist
- 汇总账户净值日报

---

## 调用链

```
总裁 → 交易意图
    ↓
模块 A：Prop Intelligence Agent
（收集规则原始信息）
    ↓
模块 B：NotebookLM Compliance Skill
（核验规则准确性，输出可信依据）
    ↓
模块 C：Strategy Pack
（判断 setup 成熟度）
    ↓
模块 D：Gatekeeper
（基于 B 的依据做最终放行/拦截）
    ↓
模块 E：Execution
（调用已跑通的自动下单链路）
    ↓
小账户真实执行 → 回写结果
```

---

## 基础设施层（独立于决策链）

- 温总（Windows PC）：执行节点
- NinjaTrader 8：执行终端
- Replikanto：账户复制和映射
- Signal Server：指令入口
- DragonFileSignal Strategy：signal.txt 读取执行

---

## 第一阶段验收条件

以下全部满足，v1 算正式成型：

- [ ] 模块 B（NotebookLM）接入，能基于原始合同输出可信规则回答
- [ ] 模块 A 能回答 Apex/MFF 的规则与流程问题
- [ ] 模块 D（Gatekeeper）能对一次交易给出放行/拦截结论
- [ ] 模块 E 在峰峰（Apex 小账户）完成至少 5 次真实执行+回写
- [ ] 龙哥-A 的系统巡检正常运行（每小时检查 SSH/内存/Server）
- [ ] 每日净值报表能自动汇总并发到电报

---

## 与 v1.0 的主要变化

| 变化 | v1.0 | v2.0 |
|---|---|---|
| Execution | 只留接口，后移 | **正式投入使用** |
| NotebookLM | 可选增强 | **必需组件，核验层** |
| 龙哥定位 | 单一角色 | **系统外工程师 + 系统内助理** |
| 验收标准 | 能回答规则和判断 setup | **能真实执行 + 结果可验证** |

---

*Version: 2.0 | 基于方案一（Plan A）| 不覆盖 v1.0*
