# 多 Agent 交易系统 正式 v1 更新稿
# Source: 龙哥（基于方案一 / Plan A，吉米协助整理）
# Version: v1.0 Updated | Date: 2026-04-07
# 状态：已确认采用
# ⚠️ 不覆盖 v1.0，本文件为正式更新版本

---

## 一、系统目标

> 在自动下单链路已跑通的前提下，正式把执行能力投入小账户实际使用，同时补强执行前的信息准确性与合规把关能力，使整个执行链达到"可用且安全"的状态。

### 本阶段目标拆解

1. 正式启用已跑通的自动下单链路
2. 引入 NotebookLM 作为合规与规则准确性增强 skill
3. 建立执行前的风控放行机制
4. 在小账户环境下进行真实执行、回写结果、持续校准
5. 把硬件与执行节点纳入基础设施层，不让其主导上层 Agent 架构

### 本阶段不追求的目标

1. 不追求完全无人值守
2. 不追求一次性做完整大系统
3. 不追求所有策略、所有资产、所有平台同时接入
4. 不追求把龙哥作为单一全能 Agent 使用
5. 不追求让 NotebookLM 直接承担执行职责

---

## 二、模块边界

### 模块 A：Prop Intelligence Agent
| 项目 | 内容 |
|---|---|
| 输入 | 平台 / 阶段 / 问题类型 / 官网/FAQ/折扣页 |
| 输出 | 当前规则 / 流程 / 活动信息 / 初步风险提示 |
| 不负责 | 最终合规裁定 / 执行放行 / 下单 |

### 模块 B：NotebookLM Compliance Skill
| 项目 | 内容 |
|---|---|
| 输入 | 官网规则文本 / 内部笔记 / 流程卡片 / 合同截图 / 问题查询 |
| 输出 | 基于来源的规则回答 / 差异比对 / 合规风险提示 / 引用依据 |
| 定位 | 信息准确性增强层，不是执行层，是风控前依据层 |
| 不负责 | 下单 / 最终放行 / 实时执行控制 |
| **重要限定** | **NotebookLM 输出的是供 Gatekeeper 使用的依据，不是最终执行裁决** |

### 模块 C：Strategy Pack
| 项目 | 内容 |
|---|---|
| 输入 | 策略名称 / 品种 / timeframe / 当前市场状态 |
| 输出 | setup 状态 / 已满足/未满足条件 / 是否接近执行 |
| 不负责 | 合规核验 / 执行放行 / 直接下单 |

### 模块 D：Gatekeeper
| 项目 | 内容 |
|---|---|
| 输入 | 交易建议 / 规则核验结果 / 账户状态 / 风险状态 |
| 输出 | 放行 / 拦截 / 降仓 / 需要人工确认 + 原因说明 |
| 不负责 | 收集原始规则 / 规则来源整理 / 直接下单 |

### 模块 E：Execution
| 项目 | 内容 |
|---|---|
| 输入 | 已放行订单 / 下单参数 / 账户平台映射 |
| 输出 | 执行结果 / 拒绝原因 / 成交记录 / 回写状态 |
| 运行策略 | 小账户 / 可控风险 / 边执行边校准 / 保留人工监控 |
| 不负责 | 规则判断 / setup 判断 / 合规判断 |

---

## 三、系统内/外角色

### 系统外：龙哥-A（首席系统工程师）
不参与交易决策链，负责系统稳定：

- 硬件监控 / 环境维护 / API 熔断 / SSH 安全
- 日志查看 / 延迟观察 / 编译运行异常处理
- GitHub / API / 通道稳定性保障

### 系统内：龙哥-B（总裁助理）
参与交易决策链，但只负责组织和传递：

- Prop 规则查询组织
- 执行前 Checklist 整理
- 账户净值日报整理
- 信息汇总与传递
- **规则准确化依赖 NotebookLM，最终放行依赖 Gatekeeper**

### 职责关系
```
龙哥    → 组织、调用、转达
NotebookLM → 规则依据准确化
Gatekeeper → 最终放行/拦截
Execution  → 执行已批准的动作
```

---

## 四、调用链

```
市场/平台规则信息
    ↓
模块 A：Prop Intelligence Agent（收集原始信息）
    ├─→ 模块 B：NotebookLM Compliance Skill（规则核验）
    └─→ 模块 C：Strategy Pack（setup 判断）
    ↓
模块 D：Gatekeeper（综合放行/拦截——同时接收规则核验与策略判断）
    ↓
模块 E：Execution（调用已跑通的自动下单链路）
    ↓
小账户真实执行 → 回写结果
```

> 说明：模块 B 和模块 C 不一定严格串行，Gatekeeper 综合两边结果再做判断。

---

## 五、第一阶段验收条件

> 系统能够基于可靠资料回答并核验 Prop Firm 规则与流程，能够根据一个已结构化策略判断 setup 成熟度，能够在执行前给出放行/拦截结论，并在小账户环境下通过已跑通的自动下单链路完成真实执行。

| 验收项 | 具体标准 |
|---|---|
| A. 规则准确性 | 能基于来源回答规则，能指出差异与风险点 |
| B. 合规依据可用 | NotebookLM 输出可用于风控的核验结果 |
| C. 策略判断可用 | 至少一个策略可被识别，能输出 setup 状态 |
| D. 执行前闸门 | Gatekeeper 能给出放行/拦截结论 + 原因 |
| E. 真实执行成立 | 小账户环境下完成执行并回写状态 |

---

## 六、基础设施执行层

此层独立于 Agent 决策链，只负责承接已批准的执行意图：

| 节点 | 说明 |
|---|---|
| 执行节点（温总）| Windows PC，运行交易软件 |
| 执行终端 | NinjaTrader，负责订单管理 |
| 账户复制层 | Replikanto，Master→Follower 账户映射 |
| Signal Server | 指令入口，接收系统下单信号 |
| Execution Adapter | 信号转译层，将指令转为平台可执行格式 |

> 注：具体实现细节（Signal Server 配置、文件路径等）见基础设施文档（infra doc），不在本主文件展开。

**此层不负责：** 判断规则、判断 setup、判断合规

---

## 七、与 v1.0 的主要变化

| 变化项 | v1.0 | v1.0 Updated |
|---|---|---|
| Execution | 只留接口，后移 | **正式投入使用** |
| NotebookLM | 可选增强 | **必需组件，核验层** |
| 龙哥定位 | 单一角色 | **系统外工程师 + 系统内助理** |
| 验收标准 | 能回答规则和判断 setup | **能真实执行 + 结果可验证** |

---

## 八、最终确认结论

1. **自动下单已跑通，立即纳入 v1 使用**
2. **NotebookLM 作为合规与规则准确性增强 skill，立即接入**
3. **龙哥不再作为单一全能 Agent，采用系统内/系统外双定位**
4. **当前重点不是继续证明能不能执行，而是让执行前判断变得可信**
5. **第一阶段在小账户环境下运行，保持风险可控、持续校准**

硬件层对系统有影响，但影响的是执行基础设施层，不是上层 Agent 逻辑。硬件是已确定载体，不是主架构的核心变量。

---

## 九、一句话版本

> v1 的目标不是等待一个完整系统成熟后再开始，而是在自动下单已可用的前提下，立即投入小账户实际使用，并通过 NotebookLM 提升规则与合规信息准确率，让整个执行链达到可用且安全的状态。

---

## 十、版本控制说明

**此文档为草稿，未经总裁审阅定稿。**

- 当前状态：待总裁 Review
- 定稿后：所有修改必须经总裁批准
- 生效条件：总裁明确确认"定稿"

---

*v1.0 Updated | 基于方案一（Plan A）| 草稿待定稿 | 2026-04-07*
