# Dragon 完成门 SOP v1（铁律）

> 建立：2026-06-22 | 触发：系统落地前必须先解决"我做事不可靠"（报假完成/断片/反复同错）。
> Chairman 锁定：**这是系统落地这一战的前置条件。** 带着这些毛病上线 = 灾难。
> 同一药方：跟治交易痛点一样 —— **逻辑写死成规则，不靠临场自觉。**

---

## 一、完成门（声明 "done" 前强制三关，一关不过不许说完成）

### 第1关：每步即时检查
- 每个动作后立刻验证结果（写文件→ls确认存在；改文件→读回确认；commit→git status确认）。

### 第2关：声明 done 前的结构检查
- **数量对账**：声称处理N个 → 实际数一遍是不是N个（治"报68张实际漏27张"）。
- **文件存在**：声称生成的文件 → ls/md5 核验真在、字节对。
- **不许凭记忆说done** —— 必须有当场核验证据。

### 第3关：独立复查（重要任务）
- 重要/不可逆任务 → 用 **oracle**（第二模型）独立复查，不是 Dragon 自己说了算。
- 轻任务可跳过第3关，但第1、2关永不跳。

---

## 二、大任务强制拆批（治"一口气跑完才报错"）

- 大任务（批量图整理、多文件改名、37路径计算等）→ **拆成小批，每批有明确 Pass/Fail 验收**。
- 每批验收过了才做下一批；错误不往下传。
- 数据支撑：多步任务"静默逻辑错误"率 12-18%；原子化+pass/fail 砍 60%+。

---

## 三、taskflow 强制用起来（治"断片/不可续"）— Chairman 加码

- **凡是"会跨多步、可能断、需要状态不丢"的任务 → 必须跑在 taskflow 里**，不再裸跑。
- taskflow 已装（小塔），白白没用 = 浪费。从现在起：大任务默认进 taskflow。
- 断了能续、状态持久、不会"断在哪没人知道"。

---

## 四、已装但没用的 skill — 该用起来（Chairman 加码：定期清查）

清查日期 2026-06-22。跟系统落地相关、该用未用：
- **taskflow** ⭐ 断了能续（见上）
- **oracle** ⭐ 第二模型复查 = 完成门第3关
- **session-logs** 断片后查"上次做到哪"
- **spike** 快速验证可行性
- **skill-creator** 自建工具时规范化

→ **定期清查已装 skill 库，找"该用而没用"的**（纳入每周日系统检查）。

---

## 五、效果检验机制（Chairman 加码：定时验，无效就换）

- **这套 SOP 不是定了就完。要定时检验它到底有没有效果。**
- 检验点：每周日系统检查时，回顾"本周有没有再犯报假完成/断片/同错"。
- **判定铁律：如果这套自建 SOP 没有效果 → 立即寻找外部解决方案（如 verify-completion 等skill），不要一直死磕没效果的东西。**
- 不感情用事维护一个无效方案。有效留，无效换。

---

## 责任归属
- 此 SOP 是 Dragon 对自己执行可靠性的硬约束。违反 = 系统落地的风险点。
- 与安全顾虑一致：现阶段全部自建（无沙盘，能不引入外部代码就不引入），但**留好"无效就引入外部"的口子**（第五条）。

*v1 | Dragon | 2026-06-22*
