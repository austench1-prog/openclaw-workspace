# Dragon Project Kickoff SOP v1
# 开展项目 SOP（通用方法论）
# Locked by Chairman 2026-06-22. Applies to PROJECTS, not daily chat / small tasks.

> **适用边界：** 这是「项目级」工作的标准开法 —— 多步骤、有结构、要做出质量的东西。
> 日常对话、单条提醒、小修小改 **不走这套**（那些直接做）。
> Chairman 判定「这是一个项目」时，Dragon 必须按本 SOP 五步走，不得跳步埋头苦干。

---

## 核心信条（Chairman 2026-06-22 锁定，本 SOP 的灵魂）

1. **结构先行，不埋头苦干。** 动手前先搞清「这项目由哪些部分组成」。没有结构 = 没有地图就上路。
2. **工具是针对性调研，不是撞运气搜索。**
   - ❌ 错误模式：「一搜就一个惊喜，不搜就埋头苦干，到时候按理解决。」
   - ✅ 正确模式：先有结构 → 知道每个部分**缺什么能力** → 才去找**填那个缺口**的工具 → 并**评估它对我们的实际影响**（不是报个新发现就完事）。
3. **高效 + 有质量地把事做出来，跟工具的关系非常大。** 选对工具 = 事半功倍；选错/不选 = 事倍功半。工具决策是项目成败的杠杆，不是附属品。
4. **最高目标优先**（沿用既有铁律）：先按最高目标（系统/自动）尝试 → 真做不到才退而求其次。

---

## 五步流程（PROJECT KICKOFF：S→T→R→E→Q）

### 第 1 步 — Structure 结构设计（项目由哪些部分组成）
**产出：一张「项目构造图/清单」。**
- 把项目拆成它的组成部分（模块/环节/子系统）。
- 每个部分写清：它要做什么、输入是什么、输出是什么、和别的部分怎么衔接。
- 标出**已有 vs 待建**、**确定 vs 待定**。
- 标出**关键缺口**（哪些部分我们还没有能力/工具做）。
- ⚠️ 不在这步选工具、不在这步排路径 —— 只把「地图」画出来。

### 第 2 步 — Tools 针对性工具调研（知道缺什么才找什么）
**产出：一张「工具决策表」。**
- **只针对第1步标出的「关键缺口」找工具** —— 不漫无目的搜。
- 每个候选工具必须回答三问（缺一不可）：
  1. **它填哪个缺口？**（对应到结构里的具体部分）
  2. **对我们的实际影响是什么？**（省多少工/提多少质/有什么代价/有什么坑）—— 不准只报「发现了个好东西」。
  3. **现有的东西能不能做？**（先查系统里已有的，别重复造）
- 工具结论分三档：**采用 / 待评估（注明评估什么）/ 排除（注明原因）**。
- 纪律：搜索结果是「线索」不是「结论」，关键限制必须**实地核实**（Chairman 经验 > 官方说法）。

### 第 3 步 — Roadmap 路径图（先后顺序 + 依赖）
**产出：一张「执行路径图」。**
- 把第1步的部分 + 第2步的工具，排成**有先后、有依赖**的执行序列。
- 标出哪步是地基（必须先做）、哪步可并行、哪步有前置条件。
- 标出每步的**验收标准**（怎么算这步做完了 = 接 Completion Gate SOP）。

### 第 4 步 — Execute 执行（按路径图，跑在护栏里）
- 严格按路径图顺序做，一步一验收。
- 大任务强制**拆批 + 每批 Pass/Fail**（接 `Dragon_Completion_Gate_SOP_v1.md`）。
- 跨多步/可能断的任务跑在 **taskflow** 里（防断片）。
- 偏离路径图要先回到第1步更新结构，不允许临时即兴改方向。

### 第 5 步 — Quality 质量门（声明完成前的关）
- 走 Completion Gate：①每步即时检查 ②声明 done 前数量对账 + 文件核验（不凭记忆）③重要任务用 oracle/独立复查。
- 项目级产出必须**有证据**（测试/回放/截图/实测读数/工具输出），不接受「应该没问题」。

---

## 反模式（明令禁止）

- ❌ 跳过结构直接动手（埋头苦干）。
- ❌ 撞运气式搜索，搜到啥报啥，不评估实际影响。
- ❌ 把官方文档/搜索说法当事实，不实地核实（尤其涉及 Chairman 有实操经验的领域）。
- ❌ 拍脑袋设参数（如 ATM 目标点位没从模型反推就填数）。
- ❌ 声明完成不做对账核验。

---

## 与既有 SOP 的关系
- 本 SOP = **项目「怎么开头、怎么推进」的总纲**。
- `Dragon_Completion_Gate_SOP_v1.md` = 第4-5步的「怎么验收、怎么声明完成」细则。
- taskflow skill = 第4步「防断片」的执行容器。
- 两者是本 SOP 的下游执行工具，不冲突。

---
*SOP v1 | Dragon | 2026-06-22 | 经 Chairman 锁定 | 第一个应用案例：ATM 落地项目*
